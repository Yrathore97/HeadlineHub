---
import type { Article } from '../lib/news/types';
import { RAIL_TOPICS } from '../lib/news/categories';

interface Props {
  trending: Article[];
}

const { trending } = Astro.props;
---

<aside class="flex flex-col gap-6 self-start lg:sticky lg:top-[100px]">
  <div class="bg-surface-elevated border border-hairline rounded-md p-5 shadow-stacked-sm">
    <h2 class="text-sm font-bold text-ink mb-3.5">Most Read</h2>
    <div class="flex flex-col gap-3.5">
      {trending.map((article, i) => (
        <a
          href={article.url}
          target="_blank"
          rel="noopener noreferrer"
          class="flex gap-3 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-strong rounded-sm"
        >
          <span class="font-mono text-lg font-bold text-stone shrink-0">{i + 1}</span>
          <span class="text-[13.5px] font-medium leading-snug text-ink">{article.title}</span>
        </a>
      ))}
    </div>

    <div class="border-t border-hairline mt-4.5 pt-4">
      <h3 class="text-xs font-semibold text-ink mb-2.5">Your topics</h3>
      <div class="flex flex-wrap gap-1.5">
        {RAIL_TOPICS.map((topic) => (
          <button
            type="button"
            data-topic-toggle={topic.label}
            aria-pressed="false"
            class="topic-chip px-3 py-1 rounded-full text-xs bg-secondary-bg text-ink-soft border-none cursor-pointer transition-colors"
          >
            {topic.label}
          </button>
        ))}
      </div>
    </div>
  </div>
</aside>

<style>
  .topic-chip.is-active {
    background-color: var(--color-primary-strong);
    color: var(--color-on-primary);
  }
</style>
